splash = true
splash_effect = 20
splash_opacity = 0.8
ipc = true

# Machine-local wallpaper config (monitor + path). Untracked — create it
# on new machines with a wallpaper { } block.
source = ~/.config/hypr/hyprpaper-local.conf
